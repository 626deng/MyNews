<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="SuperUI.NoAction" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/white</item>
        <item name="android:statusBarColor">#f00</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorLight">@color/white</item>
    </style>

    <style name="SuperButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="SuperButton.Primary">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>